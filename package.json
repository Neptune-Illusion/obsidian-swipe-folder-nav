{
	"name": "obsidian-swipe-folder-nav",
	"version": "0.1.3",
	"description": "Swipe left/right on Obsidian mobile to navigate to the previous/next note in the same folder.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"keywords": [
		"obsidian",
		"plugin",
		"swipe",
		"navigation",
		"mobile"
	],
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.20.0",
		"obsidian": "latest",
		"typescript": "^5.4.0"
	}
}
